{
    "name": "dev container",
    "build": {
        "context": ".",
        "dockerfile": "dockerfile_devcontainer"
    },
    
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-azuretools.vscode-docker",
                "mechatroner.rainbow-csv",
                "ms-python.python"
            ]
        }
    }

    // Use 'postCreateCommand' to run commands after the container is created.
    // "postCreateCommand": "pip list"
    
    // Comment out by default to use airflow user
    // "remoteUser": "root"
}